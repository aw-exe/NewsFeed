<body>

    <div class="jumbotron jumbotron-fluid">
        <div class="container">
            <h1 class="display-4 text-center">Your Saved Articles</h1>
            <p class="lead text-center"><strong>Review and add notes to your saved articles</strong></p>
        </div>
    </div>

    <div id="articles">
        {{#each articles}}
            {{> savedArticles }}
        {{/each}}
    </div>

    {{!-- Modal for notes --}}
    {{#each articles}}
        {{> modals }}
    {{/each}}

</body>